server:
  port: 8081
  servlet:
    context-path: /state-machine-demo3
spring:
  datasource:
    url: jdbc:mysql://1p.inas.club:3306/java-study?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: abcd1234
    driver-class-name: com.mysql.cj.jdbc.Driver
state-machine:
  ddl-auto: update
  management:
    enabled: true
  console:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,state-machines
logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: DEBUG
    org.springframework.jdbc.core.StatementCreatorUtils: TRACE
