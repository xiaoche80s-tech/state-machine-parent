## 1. 核心逻辑修改

- [x] 1.1 在 `StateMachine.executeLoop()` 中，当 `findNextState()` 返回空时，增加"是否有出边"的判断逻辑
- [x] 1.2 如果有出边但无匹配条件，标记为 `FAILED`，错误消息包含状态名和可用过渡目标
- [x] 1.3 如果无出边（真正的终端状态），保持 `COMPLETED` 行为不变

## 2. 测试

- [x] 2.1 编写单元测试：验证终端状态（无出边）仍标记为 `COMPLETED`
- [x] 2.2 编写单元测试：验证有出边但条件全不匹配时标记为 `FAILED`
- [x] 2.3 编写单元测试：验证错误消息包含状态名和可用过渡列表

## 3. 验证

- [x] 3.1 运行 `mvn test` 确保全部测试通过
- [x] 3.2 在 demo 应用中复现 bug 场景，确认修复后不再显示错误的 "已完成" 状态
