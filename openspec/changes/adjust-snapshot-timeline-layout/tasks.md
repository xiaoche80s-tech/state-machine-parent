## 1. IN/OUT 块折叠与摘要行

- [ ] 1.1 在 app.js 中添加快照 IO 块展开/折叠状态管理（`snapshotIoExpanded` Set）
- [ ] 1.2 修改 index.html 中 IN/OUT 块模板：默认显示摘要行，点击后展开完整 JSON
- [ ] 1.3 在 style.css 中添加摘要行样式（`.io-summary`）和折叠动画过渡

## 2. ROUTE 快照视觉统一

- [ ] 2.1 移除 `.timeline-item.route-type` 的全宽背景高亮样式
- [ ] 2.2 修改 ROUTE 快照 HTML 结构：使用与 NODE 一致的 marker + content 布局
- [ ] 2.3 修复 ROUTE 快照处的时间线连接线断裂问题

## 3. 错误信息前置

- [ ] 3.1 将 `.timeline-error` 从 IO 块之后移动到 `.timeline-header` 下方
- [ ] 3.2 为 FAILED 状态的快照 header 添加红色左边框样式
- [ ] 3.3 确保错误信息在展开/折叠状态下始终可见

## 4. 简化 IO 块头部按钮

- [ ] 4.1 将"预览"按钮替换为图标按钮（），添加 title tooltip
- [ ] 4.2 将"复制"按钮替换为图标按钮（📋），添加 title tooltip
- [ ] 4.3 调整按钮样式使其与摘要行高度一致

## 5. 侧边抽屉同步更新

- [ ] 5.1 将上述所有变更同步应用到侧边抽屉（drawer）的快照时间线模板
- [ ] 5.2 确保抽屉中的 IO 展开/折叠状态独立管理

## 6. 验证

- [ ] 6.1 在浏览器中验证长流程实例的时间线可浏览性提升
- [ ] 6.2 验证 ROUTE 快照与 NODE 快照视觉层级一致
- [ ] 6.3 验证 FAILED 快照错误信息第一时间可见
- [ ] 6.4 验证时间线连接线在所有快照类型处保持连贯
